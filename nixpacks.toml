[phases.setup]
nixPkgs = ["nodejs", "python39", "python39Packages.pip", "gcc", "libffi", "openssl"]

[phases.install]
cmds = [
  "npm install",
  "pip install --upgrade pip",
  "pip install -r backend/requirements.txt"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
